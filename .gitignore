# Claude Code local settings (machine-specific, never commit)
.claude/settings.local.json

# Editor and OS noise
.DS_Store
Thumbs.db
desktop.ini
.vscode/
.idea/
*.swp

# Hand-editing a 3.6 MB single file tends to leave copies around
*.bak
*.orig
*.rej
index.html.save*

# Only relevant if tooling is ever added back
node_modules/
*.log
.env
